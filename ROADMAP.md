# QuickClip 迭代路线

当前版本（v0.1）先守住最核心的闭环：全局快捷键 → 框选 → 截图 → 写入剪贴板 → 在富文本框中粘贴。

## v0.2：稳定性与系统适配

- 改用 `ScreenCaptureKit`，替换系统已标记为废弃的 `CGWindowListCreateImage`。
- 完整处理多显示器、不同缩放比例和跨显示器选区。
- 用应用内轻提示替代旧版系统通知，避免请求通知权限。
- 记录但不上传匿名的本地失败诊断信息，例如权限缺失或快捷键被占用。
- 增加首次启动引导：屏幕录制权限、快捷键说明、粘贴验证。

验收标准：连续 50 次截图不崩溃；主副屏、Retina 与非 Retina 屏幕的图片尺寸和选区一致。

## v0.3：截图后的轻量操作

- 放大镜与精确像素尺寸提示。
- 复制后提供短暂浮动工具条：重新截图、保存、关闭。
- 可选延迟截图（3 秒、5 秒）与窗口截图。
- 可选保存到指定目录，但默认仍只复制、不落盘。

原则：不让编辑器打断“截图后立即粘贴”的最快路径。

## v0.4：高频工作流

- 最近截图历史（本地加密或仅内存），支持再次复制。
- 标注：箭头、矩形、高亮、模糊/马赛克。
- OCR 识别并可复制纯文本。
- 自定义文件格式、命名规则和快捷键预设。

## v1.0：可分发产品

- Developer ID 签名与 Apple 公证，消除首次打开的安全拦截。
- 自动更新机制与崩溃报告的明确隐私开关。
- 中英文界面、无障碍键盘操作和完整帮助页。
- 安装、卸载与权限撤销指引。

## 取舍原则

- 默认本地处理、不上传、不自动保存。
- 优先保证截图清晰、快捷键稳定、粘贴兼容；编辑、OCR 和云功能均为后续可选项。
